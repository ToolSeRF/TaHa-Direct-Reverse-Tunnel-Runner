## راه اندازی و مدیریت تانل طاها نسخه ی دو
اسکریپت راه اندازی و مدیریت تانل طاها با پشتیبانی از دو روش تانل مستقیم و معکوس(ریورس) با امکان راه اندازی و ترکیب در +200 لایه مختلف HTTP,HTTP2,H2,TCP,MTCP,UDP,WS,WSS,MWS,MWSS,TLS,OTLS,RELAY,SOCKS5,SSH,FTCP ، به یاد طاها هوشیار کودک 12 ساله جان فدای میهن ، اسم اسکریپت طاها نام گذاری شده، این اسکریپت با ترکیب proto های مختلف که بر روی سرورهای به شدت محدود تست شده است راه فراری برای عبور از محدودیت ها و فیلترینگ شدید جمهوری اسلامی بر روی سرورها 
را فراهم میکند.

با توجه به اینکه دیدم یک سری از دوستان دارن از موقعیت برای فروش لایسنس تانل روی پروتکل HTTP استفاده میکنند، به لیست پروتکل ها اضافه شد. می تونید از ترکیب پرطرفدار و بدون مشکل http استفاده کنید.

-  بهبود پنل و تانل طاها در نسخه دو
     - اضافه شدن پروتکل های جدید به تانل طاها
     - اضافه شدن پارامترهای بیشتر برای پایداری و امنیت تانل
     - اضافه شدن قابلیت افزودن ریست سرویس توسط کرون جاب
	 - اضافه شدن optimizer حرفه ای تر
	 - مشاهده دستور cli لایو در پنل
	 - بهبود سرویس تانل ها


## 🚀 پنل تحت وب، راه اندازی چندین تانل بین چندین سرور
    wget -O taha-webui.sh "https://raw.githubusercontent.com/ToolSeRF/TaHa-Direct-Reverse-Tunnel-Runner/main/taha-webui.sh?$(date +%s)" && chmod +x taha-webui.sh && bash taha-webui.sh



1.  ابتدا با دستور بالا پنل تحت وب را در یکی از سرورها نصب کنید.
>  ***میتوانید بر روی سرور ایران یا خارج یا حتی بر روی یک سرور واسط نصب کنید،نیازی به نصب بر روی همه سرورها نیست!!! سروری که به سرورهای دیگر شما قابلیت SSH را داشته باشد را انتخاب کنید.***

2.  پس از نصب پنل با استفاده از http://ip:port وارد پنل شوید.
3.  می توانید از گزینه ی ADD SERVER برای اضافه کردن سرورهای خود به صورت دائمی استفاده کنید، همچنین در هنگام CREATE TUNNEL این امکان به صورت میانبر اضافه شده.
4.  با استفاده از گزینه ی CREATE TUNNEL برای راه اندازی یک تانل جدید اقدام کنید.
     - در مرحله اول باید انتخاب کنید که در کدام سمت هستید، اگر روی سرور ایرانی که میخواهید تانل کنید هستید i,m in iran، اگر روی سرور خارجی که میخواهید تانل کنید هستید i,m in kharej و اگر روی سرور واسط هستید از گزینه ی i,m outside استفاده کنید.
     - اگر سرور را از قبل اضافه نکردید میتوانید اطلاعات سرور یا سرورهایی که میخواهید تانل کنید را همینجا وارد کنید یا از لیست سرور ها انتخاب کنید.
     - سپس با استفاده از پرکردن فیلدها و دکمه RUN TUNNEL تانل خود را برقرار کنید.
5.  با استفاده از دکمه TUNNELS میتوانید لیست تانل هایی که ایجاد کردید را مشاهده کنید.
6.  در لیست تانل ها می توانید با استفاده از دکمه ی MANAGE تانل را مدیریت کنید، یا نسبت به تغییرات،مشاهده ی لاگ، اضافه کردن قابلیت های دیگر اقدام کنید.
7.  گزینه UNISTALL تانل منتخب را بین دو سرور حذف خواهد کرد.
8.  در صورت تغییر اطلاعات سرور از SERVERS لیست سرورها و ویرایش و ورود اطلاعات جدید اقدام کنید.


## توضضیحات CREATE TUNNEL
- ابتدا Method خود را انتخاب کنید
     - می تواند Direct یا Reverse باشد.
	 
- سپس PROTO1+PROTO2 که ترکیب لایه ها را تشکیل می دهد را تایپ کنید
     - توضیحات PORTO ها پایین تر ذکر می شود.
	 
- در ورژن دوم پنل پارامترهای اضافه نسبت به PROTO انتخابی اضافه شد تا تانل پایدارتری داشته باشید پس می توانید با استفاده از گزینه enable parametrs نسبت به فعال سازی و قرار دادن مقادیر دلخواه خودتان اقدام کنید.
     - این مورد برای مواردی مثل بالا بردن امنیت تانل، پایداری تانل در پروتکل های دارای MUX به شدت کاربردی است.
	 
- در ورژن جدید می توانید نوع پورت فوروارد خود را انتخاب کنید.
     - می تواند tcp یا udp باشد.
	 

## توضضیحات MANAGE TUNNEL
- در این بخش می توانید
     - وضعیت و پارامترهای سرویس تانل خود را در هر سرور مشاهده کنید.
	 - می توانیداز بخش CHANGE برای تغییر پروتکل ها، افزودن یا ویرایش پورت ها و ویرایش پارامترها استفاده کنید.
	 - می توانید CLI که با آن تانل اجرا شده را ببینید و از آن برای سرورهایی که دسترسی SSH ندارند استفاده کنید.
	 - می توانید لاگ سرویس های هر سرور را مشاهده کنید.
	 - با استفاده از ADD/REMOVE CRONJOB می توانید سرویس ریست برای تانل ایجاد یا حذف کنید.
	 - می توانید نسبت به ریست،استارت، استاپ سرویس ها اقدام کنید.
	 
>
>  نکته : از CLI که پنل برای نوع تانل انتخابی میسازه میتونید برای سرورهایی که بهش نمیتونید SSH بزنید استفاده کنید اگر براتون سخته CLI و پارامترهاش رو یاد بگیرید.


>  ***برای اجرای تانل نیاز به دانلود هسته یا دانلود اسکریپت طاها روی سرورهای خود ندارید فقط نسخه وب را روی یک سرور اجرا و با استفاده از آن عملیات مورد نیازتان را انجام دهید***


<img width="727" height="594" alt="image" src="https://github.com/user-attachments/assets/a66b086b-db92-4b1e-ab52-fd38e7d53971" />
<img width="712" height="599" alt="image" src="https://github.com/user-attachments/assets/b363ef17-11e6-4064-95a6-2ed9a1854c4f" />



## 🚀 نصب و اجرا به صررت منو
    wget -O taha "https://raw.githubusercontent.com/ToolSeRF/TaHa-Direct-Reverse-Tunnel-Runner/main/taha?$(date+%s)" && chmod +x taha && bash ./taha


## آموزش راه اندازی از طریق اسکریپت با منو
0.  در هر دو سرور ابتدا با گزینه 1 هسته را نصب کنید.
1.  با گزینه 2 یا 3 متد خود را انتخاب کنید، در سمت ایران و خارج مشابه.
2   سپس پروتو مد نظر خود را در این مرحله وارد کنید.
3.  سپس به سوالات به ترتیب پاسخ دهید.
4.  برای اینکه به مشکل نخورید ترتیب راه اندازی را در ایران و خارج رعایت کنید طبق نکات زیر عمل کنید. 
5.  در صورت نیاز custom paramters را y کنید و پارامترها را جهت بهبود و پایداری تانل خود وارد کنید.
## نکات
1.  برای متد Direct ابتدا تنظیمات را سمت سرور خارج و سپس سمت سرور ایران انجام دهید.
2.  برای متد REVERSE ابتدا تنظیمات را سمت ایران و سپس سمت خارج انجام دهید
3. در هر متد دقیقا SIDE که در آن قرار دارید را انتخاب کنید.
4.  سعی کنید Tunnel ID را در هر دو سمت مشابه قرار دهید، صرفا جهت متمایز کردن سرویس ها از همدیگر برای شناسایی و عدم تداخل برای راه اندازی بین چند سرور است.
5.  برای راه اندازی در پروتکل SSH ابتدا با گزینه 8 یک keygen بسازید ، برای ریورس این کار را در خارج و برای مستقیم در ایران انجام دهید. SSH ID فقط جهت تمایز بین keygen هاست.
6.  برای تنظیم کرون جاب می توانید از گرینه ی 5 استفاده کنید.سرویس ها طبق تنظیم شما ریست می شوند.
7.  برای مدیریت سرویس ها از گزینه ی 4 استفاده کنید
8. پیشنهاد میشه برای بهتر شدن وضعیت و پایداری تانل از گزینه 7) Optimizer استفاده کنید.
9. برای تنظیم کرون جاب جهت ریست سرویس از 5) Service Reset CronJob استفاده کنید.
10. برای حذف سرویس های تانل از گزینه 9) Uninstall استفاده کنید.

<img width="957" height="582" alt="image" src="https://github.com/user-attachments/assets/f430f972-538b-4f0b-9a99-926c9c421ea0" />


# راهنمای استفاده از CLI

-  توضیح -  پارامتر -  توضیح -  پارامتر -  توضیح -  پارامتر - 
-  :--- -  :---: -  :--- -  :---: -  :--- -  :---: - 
-  `نمایش منو اسکریپت` -  `.\taha .\taha -m` -   `نمایش راهنما` -  `.\taha -h` -   `راهنما پروتکل ها` -  `.\taha -p` -  

---

## ساختار دستور CLI برای اجرای مستقیم تانل

```bash
Direct:
  IRAN  : ./taha ir <method> PROTO1+PROTO2  TID KHAREJ_IP TUN_PORT FORWARD_PORTS [SSH_ID] [p=PARAMS]
  KHAREJ: ./taha kh <method> PROTO1+PROTO2  TID IRAN_IP   TUN_PORT [p=PARAMS]

Reverse:
  IRAN  : ./taha ir <method> PROTO1+PROTO2  TID KHAREJ_IP TUN_PORT [p=PARAMS]
  KHAREJ: ./taha kh <method> PROTO1+PROTO2  TID IRAN_IP   TUN_PORT FORWARD_PORTS [SSH_ID] [p=PARAMS]
```

## توضیح پارامترها

-  پارامتر -  اجباری -  توضیح - 
-  :--- -  :---: -  :--- - 
-  `side` -  ✅ -  سمت اجرا: `ir` / `iran` یا `kh` / `kharej` - 
-  `method` -  ✅ -  متد اجرا:`d` / `direct` یا `r` / `reverse` - 
-  `PROTO1+PROTO2` -  ✅ -  ترکیب پروتکل‌ها - 
-  `TID` -  ✅ -  شناسه تونل (`1` تا `99`) - 
-  `PEER_IP` -  ✅ -  آی‌پی سمت مقابل - 
-  `TUN_PORT` -  ✅ -  پورت تونل - 
-  `FORWARD_PORTS` -  ⛔/✅ -  در متد ریورس سمت kharej / در مستقیم سمت iran نمونه `udp-80` `tcp=80,2053` `udp=2050-2060`  - 
-  `SSH_ID` -  ⛔/✅ -  فقط اگر پروتکل شامل `ssh` باشد - 
-  `p=` -  ⛔/✅ -  اگر میخواهید مقادیر دلخواه برای پارامترهای پروتکل تنظیم کنید - 

---

## مثال‌های کاربردی

```bash

# Direct - سمت خارج
./taha kh direct tls+ws 9 10.20.0.41 443

# Direct - سمت ایران (forwarder)
./taha ir d tls+ws 9 10.20.30.40 443 tcp=2052,2053 p=1,2,3,true,5,6

# Reverse - - سمت ایران 
./taha iran r relay+mws 9 10.20.30.41 443

# Reverse - سمت خارج (forwarder)
./taha kharej reverse relay+mws 9 10.20.30.41 443 udp=2052,2053 p=10,9,false,0,0,0,1

# مثال SSH (نیازمند SSH_ID)
./taha ir direct ssh+raw 9 10.20.30.40 443 2052,2053 5
```

---

### نحوه استفاده از پروتکل ها
[مشاهده ی لیست مجاز قابل استفاده](https://raw.githubusercontent.com/ToolSeRF/TaHa-Direct-Reverse-Tunnel-Runner/refs/heads/main/all)

[مشاهده ی لیست و ترتیب پارامترها قابل استفاده](https://raw.githubusercontent.com/ToolSeRF/TaHa-Direct-Reverse-Tunnel-Runner/refs/heads/main/params)


### بررسی
گزارش های زیادی داشتم که دیتا رد نمیشده به هر دلیلی(محدودیت کرنل) و با اپتیمایز مشکلشون حل شده پس حدالامکان اپتیمایز رو انجام بدید
```unlimit -n```
اگر مقدار ulimit سیستم کم هست پیشنهاد میشه حتما افزایش بدید
```/etc/security/limits.conf```
مقادیر زیر رو داخلش قرار بدید
```
* soft nofile 1048576
* hard nofile 1048576
root soft nofile 1048576
root hard nofile 1048576
```
و بعد
```systemctl daemon-reexec```


اگر مشکلی دارید این دو مورد را حتما چک کنید، وضعیت سرویس ها در سرورها، وضعیت پورت ها، بخصوص در حالت ریورس حتما چک کنید پورت روی ایران باز شده باشد.

```systemctl list-units -  grep gost``` یا ```ss -ltnp -  grep ':8080'```
   
اگر پورت ها را نمیبینید (در حالت ریورس روی ایران) و سرویس شما بدون خطا ران شده نیاز به ریست سرویس هاست تا پورت بایند شود.

### هسته
از هسته GOST V3.2.27 بسیار سبک و قدرتمند استفاده شده است.

## حمایت
جهت حمایت فقط برای روح پاک کودک 12 ساله ی جان فدای میهن مان - طاها هوشیار-  و تمامی عزیزان جان فدای انقلاب دی 1404 ایران عزیزمان دعای آرامش کنید

حمایت مالی جهت ارتقا پروژه ( فقط چنانچه واقعا پروژه براتون ارزشمند بوده)

TRX/TRC20 ```TN5yCqCULRzCJeqC8ut4vSvi1yDMLT6BAH```
